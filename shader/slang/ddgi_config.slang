// Authoritative compile-time DDGI transport configuration shared by the host and shaders.
// `build.rs::generate_ddgi_config` mirrors these values into Rust.
public static const uint DDGI_RAYS_PER_PROBE = 64u;
