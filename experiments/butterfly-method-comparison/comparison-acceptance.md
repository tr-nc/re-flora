# A/B 总对比页交付验收

日期：2026-09-06。主页面：`index.html`。

预览地址：`http://127.0.0.1:8788/butterfly-method-comparison/index.html`。

本页引用 A 执行者已完成的真实 v1/v2 PNG、80px 最近邻诊断图、验收记录；不改变这些文件。B 引用同目录 `blender/` 的真实渲染、同源动画 GLB、转台视频及保存工程。所有路径相对当前目录，本地 model-viewer 脚本和许可证齐全，不依赖外部 CDN。

## 实际浏览器检查

- 在内置浏览器通过 8788 HTTP 服务实际打开，标题为“蝴蝶实测对比 · 内置生图 vs Blender”。
- A 显示“已载入真实最近邻派生图：80×80”；B 显示“已载入索引派生图：80×80 · 5项/二值alpha”。已观察两侧真实画面：A 的烘焙棋盘可见，B 透明区域透出紫色底。
- 实际暂停并设 t=.2；方向切至90°、尺度切至原始大图，状态和画面均更新。原始模式禁用 B 灰度选择，避免把蓝色原始 RGBA 错标为灰度索引。
- 实际设 t=.8 到第5帧，点击“下一帧”回 t=0/第1帧。
- 点击真实三维源模型导航，看到模型、共享动画已载入状态和正在播放的4秒真实转台视频；可见 .blend 与 GLB 下载入口。
- 控制台 error 记录为空。

## 已保留的验收边界

- A 本次两次生成均不是真透明，相位/锚点和循环不合格；不能由降采样推断可以导入。
- B 一键重建和“导出已编辑工程”入口均实跑；25/25保存工程重放逐RGBA一致，t=0/t=1端点一致、无贴边，单条GLB动画包含左右翅双通道，0..1秒。
- B 16px格式通过，不代表艺术质量通过：正/背部分帧仅8个不透明像素，身体/触角容易消失。未替换正式游戏资产，也未运行游戏导入。
- 总页共用帧号，但明确不把 A 的实际姿势称为与 B 同相位。
- 子任务无法设置内置浏览器 visible；本次是实际隐藏浏览器验证。主任务需打开上述总页供用户查看，不得将本次后台检查称为已向用户显示。

具体细节与产物来源分别见 `ai-route-a-acceptance.md`、`blender/README.md`、两条路线的 manifest 和 `blender/validation.json`。
