Use case: precise-object-edit
Asset type: experimental pixel-art butterfly animation spritesheet, not a presentation or contact-sheet mockup.
Input images: Image 1 is the edit target, the previous 5x5 blue butterfly candidate. Image 2 is the real 80x80 game spritesheet and is a low-resolution layout/silhouette reference. Image 3 is the existing Blue.png color reference.
Primary request: correct the candidate's background and animation registration while preserving its blue/cyan/purple butterfly design. The previous apparent transparency is WRONG: the grey checkerboard is painted into RGB. Remove all of that background and deliver real PNG alpha transparency. Outside butterfly pixels alpha must equal zero. Do not draw a checkerboard, white rectangle, or any other background. Transparent cutouts only.
Composition: exactly 25 butterflies in a mathematically regular 5-column x 5-row square grid, ideally a 1000x1000 canvas divided into 200x200 cells. The thorax pivot of every butterfly must sit at the exact center of its own cell. Identical body length, wing length and apparent scale in all 25 cells; consistent clear cell padding. No title, text, labels, borders, guides, grids, margin bars, drop shadows, ground, glow, or other objects.
Animation repair: the columns are five samples of ONE periodic wingbeat, not a sequence of progressively shrinking wings. Column phases are 0.0, 0.2, 0.4, 0.6, 0.8 cycles. Corresponding signed hinge angles are 0 degrees flat/open, +57 degrees upward, +35 degrees upward, -35 degrees downward, -57 degrees downward. Column 5 smoothly loops into column 1. The thorax stays still; the wings rotate around the same hinges without changing shape, markings or size. Crucially each column must represent the EXACT SAME signed wing angle in EVERY row.
Views: row 1 front at azimuth 0 degrees, row 2 front-quarter 45 degrees, row 3 side 90 degrees, row 4 rear-quarter 135 degrees, row 5 rear 180 degrees. Use the same moderately elevated orthographic camera for all rows, only azimuth changes. Do not confuse camera turn with flap phase. Preserve one identical butterfly anatomy and markings across these viewpoints.
Style invariants: preserve the reference's blue/cyan forewings, purple lower wings, dark compact thorax and antennae, restrained pale highlight; chunky crisp pixel-art silhouette legible at 16x16 pixels per frame; flat limited colors with hard edges, no gradients, no antialiasing, no invented details. Do not shrink or enlarge the body between frames. This is a real transparent game sprite asset, not an illustration of a spritesheet on a surface.
