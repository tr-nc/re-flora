# 蝉鸣无遮挡诊断试用（2026-09-14）

用户报告移动时树上尖锐叫声伴有轻微爆音，不方便录制，希望先关闭遮挡对比。
仅 Cicadas 单次播放改为 DirectGeometry::BypassTransmission：
仍是世界坐标单点声源，保留方向、距离衰减、音量、原素材、环境路由和生命周期。
树叶声及其它声音不变。此为明确的诊断试用，不代表遮挡已被证明是根因。

fmt/check、979 主测试 + 4 库测试（2 ignored）通过；
新增测试验证蝉鸣直达路径 bypass，其他类别继续使用默认世界路径。
持锁 release hidden muted 启动验证正常退出、failures=0、无 ERROR，
保留多蝴蝶 atlas 选择警告：
target/re-flora-logs/re-flora-20260914-004243.122-190167.log。
短静音 smoke 不证明蝉鸣播放或移动爆音已经解决，须人工试听。
生成文件未改；用户 config/gui.toml 的已有修改保持不动。

同时解释叶子局部运动：leafLocalOffset 的臂长为 0.5 体素，
Flutter Strength 改变有饱和的扭转目标，不直接调整位移幅度。
它同时影响局部位置和光学朝向；固定风会收敛，并非持续周期摇摆。
现有固定风测试 peak_angle=0.21482rad，位置变化约 0.1 体素量级，
因此看不出明显位置抖动是合理的；本次没有修改该动画模型或滑杆。
